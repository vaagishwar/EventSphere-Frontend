export const selectBookings = (state) => state.bookings.items;
export const selectBookingStatus = (state) => state.bookings.status;
export const selectLastCreatedBooking = (state) => state.bookings.lastCreated;
